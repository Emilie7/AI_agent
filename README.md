This is a project on creating a toy AI in python from the Boot.dev cours "Build an AI Agent in Python". ¨

Running this project as is can be dangerouse as the AI has the ability to run Python code on your machin through 
the subrutine method. Please insure that all the python files in functions are limited to work within the folder
you expect by looking at call_function.py. By defualt the code should only work when calling methods on files and
folders within the calculator directory of this project.
